// ltl-z80-emu/main.ltl
//
// Z80 CPU emulator passing ZEXDOC/ZEXALL.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-z80-emu v0.1.0")
    io::println("Z80 CPU emulator passing ZEXDOC/ZEXALL.")
    0
}
