// types/shared.ltl — shared type aliases for ltl_z80_emu

module ltl_z80_emu::types

pub type Id = int
pub type Name = str
pub type Payload = list<int>

pub enum Status {
    Ok,
    Pending,
    Failed { reason: str },
}

pub struct Meta {
    id: Id,
    name: Name,
    status: Status,
}