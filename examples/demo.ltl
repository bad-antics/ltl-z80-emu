// examples/demo.ltl — runnable demo for ltl_z80_emu

import std::io
use ltl_z80_emu as lib

fn main() {
    io::println("ltl_z80_emu demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}